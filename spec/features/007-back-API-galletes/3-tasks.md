# Tareas — 007 API de galletas

⚠️ **CANCELADA** — El frontend usa catálogo estático en HTML. La API existe (`backend/routes/cookies.js`) pero no se usa desde el frontend.

- [ ] T1 — `backend/routes/cookies.js`: crear router con `GET /api/cookies` (pública, sin middleware de sesión).
- [ ] T2 — Query SELECT de todas las cookies ordenadas por id, retornando todos los campos necesarios.
- [ ] T3 — Montar el router en server.js.
- [ ] T4 — Verificar: `GET /api/cookies` retorna 200 con array de 6 galletas con los campos correctos; sin sesión funciona igual.
- [ ] T5 — Si todo pasa, roadmap 007 a «hecho».
